const mongoose = require('mongoose');

const mockDbConnect = async () => {
    console.log('Mock database connected');
    // Optionally, mock mongoose models and methods if needed
};

module.exports = { mockDbConnect };
